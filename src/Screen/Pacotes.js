import React from 'react'
import {View, StyleSheet, Text} from 'react-native'
import MenuHeader from '../Components/MenuHeader'

const Pacotes = ()=>{
    return(
        <View>
            <MenuHeader title='Pacotes'/>
            <Text>Pacotes</Text>
        </View>
    )
}

const styles = StyleSheet.create({})

export default Pacotes